import { default as Auth } from './auth'
import { default as CheckingAccount } from './checking-account'
import { default as CreditCard } from './credit-card'
import { default as Expense } from './expense'
import { default as Home } from './home'

const Pages = {
	Auth,
	CreditCard,
	CheckingAccount,
	Expense,
	Home
}

export default Pages
