import { LocalStorage } from './local-storage.utils'
import { Recharts } from './recharts.utils'
import { SessionStorage } from './session-storage.utils'

export const Utils = {
	SessionStorage,
	LocalStorage,
	Recharts
}
